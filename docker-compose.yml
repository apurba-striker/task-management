version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development
      - PORT=5000
      - MONGODB_URI=mongodb+srv://apurbapatra12:apurbaP@Cluster0.dovgxio.mongodb.net/taskmanagement
      - JWT_SECRET=abcedddsodadsada556547
      - JWT_EXPIRES_IN=7d
      - FRONTEND_URL=http://localhost:3000

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:5000/api
    depends_on:
      - backend
